[project]
name = "ChaoticMinimaxH3Director"
version = "1.0.0"
description = "Timeline-based MiniMax H3 director: VRAM-safe chunked rendering with frame-accurate seam continuity."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[project.optional-dependencies]
test = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
